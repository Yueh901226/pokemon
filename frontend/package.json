{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "sync": "git add .. && git commit -m \"Auto update code and deploy\" && git push origin master && npm run deploy"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "opencc-js": "^1.4.0",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "gh-pages": "^6.3.0",
    "vite": "^8.1.1"
  }
}
